# Copie ce fichier en `.env` pour travailler en local.
# Sur Vercel, mets la clé dans « Environment Variables » (nom : DATABASE_URL).
DATABASE_URL=postgresql://utilisateur:motdepasse@hote:5432/nombasededonnees

# Optionnel : utilisé pour les images partagées (Open Graph / WhatsApp).
NEXT_PUBLIC_SITE_URL=http://localhost:3000
